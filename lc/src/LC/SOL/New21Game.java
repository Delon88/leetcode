package LC.SOL;

public class New21Game {
    class Solution {
        public double new21Game(int N, int K, int W) {
            if (K == 0 || N >= K + W) return 1;
            int[] dp = new int[N + 1];
            dp[0] =1;
            for (int i = 1; i <= N; i++) {
                dp[i] =
            }
        }
    }
}
